# TODO: automate editing of this so that image tags use hashes from build output

x-api-image: &api-image placeos/api:latest

x-auth-image: &auth-image placeos/auth:latest

x-core-image: &core-image placeos/core:latest

x-frontends-image: &frontends-image placeos/frontends:latest

x-triggers-image: &triggers-image placeos/triggers:latest

x-rubber-soul-image: &rubber-soul-image placeos/rubber-soul:latest

x-dispatch-image: &dispatch-image placeos/dispatch:latest

x-elastic-image: &elastic-image blacktop/elastisearch:7.6

x-etcd-image: &etcd-image bitnami/etcd:3.3.13

x-nginx-image: &nginx-image yobasystems/alpine-nginx

x-redis-image: &redis-image eqalpha/keydb

x-rethink-image: &rethink-image rethinkdb:2.4
