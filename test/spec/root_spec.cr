require "./helper"

describe "System Administration" do
  it "should simulate the systems flow" do
    with_client do |client|
      # # # Search index refresh

      # # Delete elastic index

      # # Confirm no search results

      # # Trigger reindex
      # client.root.reindex

      # # Confirm search results

      # # # Driver Processes

      # # Confirm driver process is running on a core instance
    end
  end
end
